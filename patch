diff --git a/chatdraw/chat_system.py b/chatdraw/chat_system.py
index 6f914b9..b7f9363 100644
--- a/chatdraw/chat_system.py
+++ b/chatdraw/chat_system.py
@@ -1,4 +1,4 @@
-from typing import Dict, Any
+from typing import Dict
 from abc import ABC, abstractmethod
 from pydantic import BaseModel
 
@@ -58,9 +58,23 @@ class ChatHandler:
             )
         )
 
+        # in case project ends the context is empty 
         projects_next_context = ""
+        # if a project continues the response is non empty
         if project_response.next_context!="":
             projects_next_context = current_project + "_" + project_response.next_context
+        # if the project dives into another project, we process a different message
+        if "." in projects_next_context:
+            next_respose = self.process_message(
+                ChatMessage(
+                    message="",
+                    context=".".join(context_list[:-1] + [projects_next_context]),
+                )
+            )
+            return ChatResponse(
+                response=project_response.response + next_respose.response,
+                next_context=next_respose.next_context,
+            )
         return ChatResponse(
             response=project_response.response,
             next_context=".".join(context_list[:-1] + [projects_next_context]),
diff --git a/chatdraw/chatflows/greeting.py b/chatdraw/chatflows/greeting.py
index 24c7aac..9de8501 100644
--- a/chatdraw/chatflows/greeting.py
+++ b/chatdraw/chatflows/greeting.py
@@ -1,6 +1,6 @@
 from enum import Enum
 from typing import Any, Dict, List
-from chatdraw.chat_system import ChatMessage, ChatResponse, ProjectHandler
+from chatdraw.chat_system import ChatHandler, ChatMessage, ChatResponse, ProjectHandler
 
 
 class GreetingProjectStates(Enum):
@@ -31,5 +31,7 @@ class GreetingProject(ProjectHandler):
     def _choose_project(self, message:str) -> ChatResponse:
         return ChatResponse(response="",next_context="")
 
-
+if __name__=="__main__":
+    ch = ChatHandler()
+    ch.register_project("greeting",GreetingProject(available_projects=[]))
     
\ No newline at end of file
